<!DOCTYPE html>
        <html lang="zh-TW">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>VS Schedule Tool - No-Scroll TestCase Summary Report</title>
            <style>
                
                :root {
                    --primary-color: #667eea;
                    --secondary-color: #764ba2;
                    --accent-color: #4a90e2;
                    --text-dark: #1a365d;
                    --bg-light: #f8fafc;
                    --border-color: #e8ecf0;
                    --success-color: #28a745;
                    --success-bg: #E8F5E8;
                    --success-text: #155724;
                    --error-color: #dc3545;
                    --error-bg: #FFE8E8;
                    --error-text: #721c24;
                    --info-color: #007bff;
                    --info-bg: #f8fbff;
                }

                * { 
                    margin: 0; 
                    padding: 0; 
                    box-sizing: border-box; 
                }

                body { 
                    font-family: 'Segoe UI', 'Microsoft JhengHei', 'Arial', sans-serif; 
                    background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%); 
                    min-height: 100vh; 
                    padding: clamp(8px, 2vw, 25px);
                    line-height: 1.5;
                }

                .container { 
                    background: white; 
                    border-radius: clamp(8px, 1.5vw, 16px); 
                    box-shadow: 0 10px 30px rgba(0,0,0,0.15); 
                    overflow: hidden; 
                    max-width: clamp(600px, 90vw, 1400px); 
                    margin: 0 auto; 
                }

                .header { 
                    background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%); 
                    color: white; 
                    text-align: center; 
                    padding: clamp(20px, 4vw, 35px) clamp(15px, 3vw, 30px); 
                }

                .title { 
                    font-size: clamp(22px, 4.5vw, 36px); 
                    font-weight: 700; 
                    margin-bottom: clamp(6px, 1.2vw, 12px); 
                }

                .subtitle { 
                    font-size: clamp(13px, 2.5vw, 20px); 
                    opacity: 0.9; 
                    margin-bottom: clamp(10px, 2vw, 16px); 
                }

                .product-info { 
                    background: rgba(255, 255, 255, 0.1); 
                    border-radius: clamp(6px, 1.2vw, 12px); 
                    padding: clamp(12px, 2.5vw, 20px); 
                    margin-top: clamp(12px, 2vw, 20px); 
                    border: 1px solid rgba(255, 255, 255, 0.2); 
                }

                .product-info h3 { 
                    color: white; 
                    margin-bottom: clamp(6px, 1vw, 10px); 
                    font-size: clamp(14px, 2.5vw, 18px);
                }

                .product-info p { 
                    color: white; 
                    font-size: clamp(12px, 2vw, 16px);
                    font-weight: 500;
                }

                .statistics-section { 
                    padding: clamp(15px, 3vw, 25px) clamp(15px, 2.5vw, 25px); 
                    background: white; 
                    color: black; 
                    border-bottom: clamp(3px, 0.5vw, 5px) solid var(--text-dark); 
                }

                .statistics-section h3 {
                    text-align: center; 
                    margin-bottom: clamp(10px, 2vw, 15px);
                    font-size: clamp(16px, 3vw, 24px);
                    color: var(--text-dark);
                }

                .stats-grid { 
                    display: grid; 
                    grid-template-columns: repeat(auto-fit, minmax(clamp(140px, 20vw, 200px), 1fr)); 
                    gap: clamp(12px, 2.5vw, 20px); 
                    margin-top: clamp(12px, 2vw, 20px); 
                }

                .stat-card { 
                    background: white; 
                    border: clamp(2px, 0.4vw, 4px) solid var(--text-dark); 
                    color: black; 
                    padding: clamp(12px, 2.5vw, 20px); 
                    border-radius: clamp(6px, 1.2vw, 12px); 
                    text-align: center;
                    transition: transform 0.2s ease, box-shadow 0.2s ease;
                }

                .stat-card:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 6px 20px rgba(0,0,0,0.1);
                }

                .stat-card.pass { 
                    border-color: var(--success-color); 
                    background: var(--info-bg); 
                }

                .stat-card.fail { 
                    border-color: var(--error-color); 
                    background: #fff8f8; 
                }

                .stat-card.rate { 
                    border-color: var(--info-color); 
                    background: var(--info-bg); 
                }

                .stat-value { 
                    font-size: clamp(24px, 5vw, 42px); 
                    font-weight: 700; 
                    margin-bottom: clamp(4px, 0.8vw, 8px); 
                    line-height: 1;
                }

                .stat-label { 
                    font-size: clamp(11px, 2vw, 16px); 
                    color: #666; 
                    font-weight: 500; 
                }

                /* 🚫 移除捲軸 - 表格完全適應容器 */
                .table-container { 
                    padding: clamp(15px, 3vw, 25px); 
                    /* 移除 overflow-x: auto 和 overflow-y: auto */
                    /* 移除 max-height 限制 */
                }

                table { 
                    width: 100%; 
                    border-collapse: collapse; 
                    background: white; 
                    border-radius: clamp(6px, 1vw, 10px);
                    overflow: hidden;
                    /* 移除 min-width，讓表格完全適應容器 */
                    table-layout: fixed; /* 固定表格佈局，確保欄位寬度控制 */
                }

                /* 精確的欄位寬度控制 */
                .tc-no-header { width: clamp(60px, 8vw, 80px); }
                .tc-name-header { width: auto; } /* 自動寬度，佔用剩餘空間 */
                .tc-checkpoints-header { width: clamp(80px, 12vw, 120px); }
                .tc-result-header { width: clamp(70px, 10vw, 100px); }

                th { 
                    background: linear-gradient(135deg, var(--accent-color) 0%, #357abd 100%); 
                    color: white; 
                    font-weight: 600; 
                    padding: clamp(10px, 2vw, 18px) clamp(8px, 1.5vw, 15px); 
                    text-align: center; 
                    border: 1px solid #ccc; 
                    font-size: clamp(11px, 2vw, 16px);
                    /* 移除 position: sticky - 避免捲動問題 */
                }

                td { 
                    padding: clamp(8px, 1.5vw, 15px) clamp(6px, 1.2vw, 12px); 
                    border: 1px solid var(--border-color); 
                    text-align: left; 
                    font-size: clamp(11px, 2vw, 16px); 
                    line-height: 1.4; 
                    /* 文字自動換行而非隱藏 */
                    overflow-wrap: break-word;
                    word-wrap: break-word;
                }

                .tc-no { 
                    text-align: center; 
                    font-weight: 600; 
                    background: var(--bg-light); 
                }

                .tc-name { 
                    font-weight: 500; 
                    /* 移除 max-width 和 text-overflow */
                    /* 允許文字自然換行 */
                    white-space: normal;
                    word-break: break-word;
                }

                .tc-result { 
                    text-align: center; 
                    font-weight: 600; 
                }

                .tc-checkpoints { 
                    text-align: center; 
                    font-family: 'Consolas', 'Monaco', monospace; 
                    font-size: clamp(10px, 1.8vw, 15px); 
                    font-weight: 600;
                }

                .result-pass { 
                    background-color: var(--success-bg) !important; 
                    color: var(--success-text) !important; 
                    border-radius: clamp(3px, 0.6vw, 6px); 
                }

                .result-fail { 
                    background-color: var(--error-bg) !important; 
                    color: var(--error-text) !important; 
                    border-radius: clamp(3px, 0.6vw, 6px); 
                }

                tbody tr:nth-child(even) { background-color: var(--bg-light); }
                tbody tr:hover { 
                    background: #e3f2fd; 
                    transform: scale(1.002); 
                    transition: all 0.2s ease; 
                }

                .footer { 
                    background: #2c3e50; 
                    color: white; 
                    text-align: center; 
                    padding: clamp(15px, 3vw, 25px); 
                    font-size: clamp(10px, 1.8vw, 14px); 
                }

                /* 低解析度優化 800x600 - 1024x768 */
                @media screen and (max-width: 1024px) and (min-width: 769px) {
                    .container { max-width: 95vw; }
                    .stats-grid { grid-template-columns: repeat(2, 1fr); }
                    th, td { 
                        font-size: clamp(10px, 1.5vw, 12px); 
                        padding: clamp(6px, 1vw, 10px); 
                    }
                    .title { font-size: clamp(18px, 3.5vw, 24px); }
                    .subtitle { font-size: clamp(11px, 2vw, 14px); }
                    /* 確保表格在小螢幕上也不會有捲軸 */
                    .tc-name { font-size: clamp(9px, 1.5vw, 11px); }
                }

                /* 標準解析度 1280x720 - 1366x768 */
                @media screen and (min-width: 1025px) and (max-width: 1400px) {
                    .container { max-width: 92vw; }
                    .stats-grid { grid-template-columns: repeat(4, 1fr); }
                }

                /* Full HD 1920x1080 */
                @media screen and (min-width: 1401px) and (max-width: 1920px) {
                    .container { max-width: 85vw; }
                    .stats-grid { grid-template-columns: repeat(4, 1fr); }
                }

                /* 2K解析度 2560x1440 */
                @media screen and (min-width: 1921px) and (max-width: 2560px) {
                    .container { max-width: 80vw; }
                    .stats-grid { grid-template-columns: repeat(4, 1fr); }
                    .title { font-size: clamp(28px, 4vw, 38px); }
                    .subtitle { font-size: clamp(16px, 2.5vw, 22px); }
                    .stat-value { font-size: clamp(32px, 5.5vw, 48px); }
                }

                /* 4K解析度 3840x2160 及以上 */
                @media screen and (min-width: 2561px) {
                    .container { max-width: 75vw; }
                    .stats-grid { 
                        grid-template-columns: repeat(4, 1fr); 
                        gap: clamp(20px, 3vw, 30px);
                    }
                    .title { font-size: clamp(32px, 4.5vw, 44px); }
                    .subtitle { font-size: clamp(18px, 3vw, 26px); }
                    .stat-value { font-size: clamp(36px, 6vw, 52px); }
                    th, td { 
                        padding: clamp(15px, 2.5vw, 22px) clamp(12px, 2vw, 18px); 
                    }
                }

                /* 超寬螢幕 21:9 優化 */
                @media screen and (min-aspect-ratio: 21/9) {
                    .container { max-width: 70vw; }
                    .stats-grid { 
                        grid-template-columns: repeat(4, 1fr);
                        gap: clamp(15px, 2.5vw, 25px);
                    }
                }

                /* 直立螢幕優化 */
                @media screen and (max-aspect-ratio: 4/5) {
                    .stats-grid { grid-template-columns: repeat(2, 1fr); }
                }

                /* 高DPI螢幕優化 */
                @media screen and (-webkit-min-device-pixel-ratio: 2), 
                       screen and (min-resolution: 192dpi) {
                    th, td { border-width: 0.5px; }
                    .container { box-shadow: 0 8px 25px rgba(0,0,0,0.12); }
                    .stat-card { border-width: 1.5px; }
                }

                /* 效能優化 - 低解析度 */
                @media screen and (max-width: 1024px) {
                    tbody tr:hover { 
                        transform: none; 
                        transition: background-color 0.1s ease; 
                    }
                    .stat-card:hover {
                        transform: none;
                    }
                }

                /* 手機版優化 */
                @media screen and (max-width: 768px) {
                    .stats-grid { 
                        grid-template-columns: repeat(2, 1fr);
                        gap: clamp(10px, 3vw, 15px);
                    }
                    .container { border-radius: clamp(6px, 2vw, 12px); }

                    /* 手機版表格特別優化 */
                    th, td { 
                        padding: clamp(6px, 2vw, 10px) clamp(4px, 1.5vw, 8px); 
                        font-size: clamp(10px, 2.5vw, 14px);
                    }

                    .tc-no-header { width: clamp(50px, 12vw, 70px); }
                    .tc-checkpoints-header { width: clamp(70px, 15vw, 90px); }
                    .tc-result-header { width: clamp(60px, 12vw, 80px); }
                }

                /* 小手機版優化 */
                @media screen and (max-width: 480px) {
                    .stats-grid { 
                        grid-template-columns: 1fr 1fr;
                        gap: 8px;
                    }
                    .stat-card { padding: clamp(8px, 3vw, 15px); }

                    /* 極小螢幕表格優化 */
                    th, td {
                        padding: clamp(4px, 2vw, 8px) clamp(3px, 1.5vw, 6px);
                        font-size: clamp(9px, 2.2vw, 12px);
                    }

                    .tc-no-header { width: clamp(40px, 10vw, 60px); }
                    .tc-checkpoints-header { width: clamp(60px, 12vw, 80px); }
                    .tc-result-header { width: clamp(50px, 10vw, 70px); }

                    .tc-checkpoints { font-size: clamp(8px, 2vw, 11px); }
                }

                /* 列印樣式 */
                @media print {
                    body { 
                        background: white; 
                        padding: 0; 
                        font-size: 12pt; 
                    }
                    .container { 
                        box-shadow: none; 
                        border-radius: 0; 
                        max-width: 100%; 
                    }
                    .header { 
                        background: var(--primary-color) !important; 
                        -webkit-print-color-adjust: exact;
                        color-adjust: exact;
                    }
                    .stats-grid { 
                        grid-template-columns: repeat(4, 1fr);
                        page-break-inside: avoid;
                    }
                    table { 
                        font-size: 10pt; 
                        width: 100%;
                    }
                    th, td { 
                        padding: 6pt 8pt; 
                        font-size: 9pt; 
                    }
                    .footer { 
                        background: #2c3e50 !important; 
                        -webkit-print-color-adjust: exact;
                        color-adjust: exact;
                    }
                }
            
            </style>
        </head>
        <body>
            <div class="container">
                <div class="header">
                    <h1 class="title">VS Schedule Tool - Summary Report</h1>
                    <p class="subtitle">Test Results Overview - 2025/08/08 23:00:56</p>
                    <div class="product-info">
                        <h3>Product Information</h3>
                        <p>vDisplay Manager v2741.22.2000.0</p>
                    </div>
                </div>

                <div class="statistics-section">
                    <h3>📊 Summary Statistics</h3>
                    <div class="stats-grid">
                        <div class="stat-card">
                            <div class="stat-value">10</div>
                            <div class="stat-label">Total TestCases</div>
                        </div>
                        <div class="stat-card pass">
                            <div class="stat-value" style="color: var(--success-color);">9</div>
                            <div class="stat-label">Passed</div>
                        </div>
                        <div class="stat-card fail">
                            <div class="stat-value" style="color: var(--error-color);">1</div>
                            <div class="stat-label">Failed</div>
                        </div>
                        <div class="stat-card rate">
                            <div class="stat-value" style="color: var(--info-color);">90.0%</div>
                            <div class="stat-label">Pass Rate</div>
                        </div>
                    </div>
                </div>

                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th class="tc-no-header">TC No.</th>
                                <th class="tc-name-header">TestCase Name</th>
                                <th class="tc-checkpoints-header">Checkpoints</th>
                                <th class="tc-result-header">Result</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td class="tc-no">1</td>
                                <td class="tc-name">Switch to different VG model. And set the brightness &amp; Contrast = 0 on both models. And check the reest function on both VG model.</td>
                                <td class="tc-checkpoints">8/8</td>
                                <td class="tc-result result-pass">PASS</td>
                            </tr>
                            <tr>
                                <td class="tc-no">2</td>
                                <td class="tc-name">Check the monitor information. Such as SN/Model name/Firmware/Resolution.</td>
                                <td class="tc-checkpoints">1/1</td>
                                <td class="tc-result result-pass">PASS</td>
                            </tr>
                            <tr>
                                <td class="tc-no">3</td>
                                <td class="tc-name">Change the input selection.</td>
                                <td class="tc-checkpoints">10/10</td>
                                <td class="tc-result result-pass">PASS</td>
                            </tr>
                            <tr>
                                <td class="tc-no">4</td>
                                <td class="tc-name">Change the screen pivot.</td>
                                <td class="tc-checkpoints">4/6</td>
                                <td class="tc-result result-fail">FAIL</td>
                            </tr>
                            <tr>
                                <td class="tc-no">5</td>
                                <td class="tc-name">Change the Brightness value.</td>
                                <td class="tc-checkpoints">4/4</td>
                                <td class="tc-result result-pass">PASS</td>
                            </tr>
                            <tr>
                                <td class="tc-no">6</td>
                                <td class="tc-name">Change the Contrast value.</td>
                                <td class="tc-checkpoints">4/4</td>
                                <td class="tc-result result-pass">PASS</td>
                            </tr>
                            <tr>
                                <td class="tc-no">7</td>
                                <td class="tc-name">Change the Sharpness value.</td>
                                <td class="tc-checkpoints">4/4</td>
                                <td class="tc-result result-pass">PASS</td>
                            </tr>
                            <tr>
                                <td class="tc-no">8</td>
                                <td class="tc-name">Change the Volume value.</td>
                                <td class="tc-checkpoints">4/4</td>
                                <td class="tc-result result-pass">PASS</td>
                            </tr>
                            <tr>
                                <td class="tc-no">9</td>
                                <td class="tc-name">Change the View Mode value.</td>
                                <td class="tc-checkpoints">26/26</td>
                                <td class="tc-result result-pass">PASS</td>
                            </tr>
                            <tr>
                                <td class="tc-no">10</td>
                                <td class="tc-name">Change the Color Temperture.</td>
                                <td class="tc-checkpoints">10/10</td>
                                <td class="tc-result result-pass">PASS</td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <div class="footer">
                    <p>📋 Generated by VS Schedule Tool | 🕒 2025/08/08 23:00:56 | 📁 TestCase_Summary_20250808_230056.html</p>
                    <p>🎯 vDisplay Manager v2741.22.2000.0 - TestCase Summary Report</p>
                    <p style="margin-top: 5px; opacity: 0.8;">📱 No-scroll design optimized for all resolutions</p>
                </div>
            </div>
            
        <script>
        function adaptSummaryToResolution() {
            const width = window.innerWidth;
            const height = window.innerHeight;
            const pixelRatio = window.devicePixelRatio || 1;
            const aspectRatio = (width / height).toFixed(2);

            console.log(`[No-Scroll Summary] ${width}x${height}, DPI: ${pixelRatio}x, Aspect: ${aspectRatio}`);

            // 動態調整容器寬度
            const container = document.querySelector('.container');
            if (container) {
                if (width >= 3840) {
                    container.style.maxWidth = '70vw';
                } else if (width >= 2560) {
                    container.style.maxWidth = '75vw';
                } else if (width >= 1920) {
                    container.style.maxWidth = '80vw';
                } else if (width <= 1024) {
                    container.style.maxWidth = '95vw';
                }
            }

            // 根據解析度調整統計卡片佈局
            const statsGrid = document.querySelector('.stats-grid');
            if (statsGrid) {
                if (width <= 480) {
                    statsGrid.style.gridTemplateColumns = '1fr 1fr';
                    statsGrid.style.gap = '8px';
                } else if (width <= 768) {
                    statsGrid.style.gridTemplateColumns = 'repeat(2, 1fr)';
                } else {
                    statsGrid.style.gridTemplateColumns = 'repeat(4, 1fr)';
                }
            }

            // 表格欄位寬度動態調整（確保無捲軸）
            const table = document.querySelector('table');
            if (table) {
                const ths = table.querySelectorAll('th');
                const tds = table.querySelectorAll('td');

                if (width <= 480) {
                    // 極小螢幕：壓縮所有欄位
                    if (ths[0]) ths[0].style.width = '15%';  // TC No.
                    if (ths[1]) ths[1].style.width = '45%';  // TC Name
                    if (ths[2]) ths[2].style.width = '20%';  // Checkpoints
                    if (ths[3]) ths[3].style.width = '20%';  // Result
                } else if (width <= 768) {
                    // 手機：適中壓縮
                    if (ths[0]) ths[0].style.width = '12%';  // TC No.
                    if (ths[1]) ths[1].style.width = '50%';  // TC Name
                    if (ths[2]) ths[2].style.width = '18%';  // Checkpoints
                    if (ths[3]) ths[3].style.width = '20%';  // Result
                } else if (width <= 1024) {
                    // 小螢幕：輕微壓縮
                    if (ths[0]) ths[0].style.width = '10%';  // TC No.
                    if (ths[1]) ths[1].style.width = '55%';  // TC Name
                    if (ths[2]) ths[2].style.width = '17%';  // Checkpoints
                    if (ths[3]) ths[3].style.width = '18%';  // Result
                } else {
                    // 大螢幕：舒適佈局
                    if (ths[0]) ths[0].style.width = '8%';   // TC No.
                    if (ths[1]) ths[1].style.width = '60%';  // TC Name
                    if (ths[2]) ths[2].style.width = '16%';  // Checkpoints
                    if (ths[3]) ths[3].style.width = '16%';  // Result
                }
            }

            // 高DPI螢幕優化
            if (pixelRatio >= 2) {
                const elements = document.querySelectorAll('th, td, .stat-card');
                elements.forEach(el => {
                    if (el.tagName.toLowerCase() === 'th' || el.tagName.toLowerCase() === 'td') {
                        el.style.borderWidth = '0.5px';
                    } else {
                        el.style.borderWidth = '1.5px';
                    }
                });
            }

            // 效能優化
            const totalPixels = width * height;
            if (totalPixels < 1000000) {
                // 低解析度簡化動畫
                const style = document.createElement('style');
                style.textContent = `
                    tbody tr:hover, .stat-card:hover { 
                        transform: none !important; 
                        transition: background-color 0.1s ease !important; 
                    }
                `;
                document.head.appendChild(style);
            } else if (totalPixels > 8000000) {
                // 4K+ 增強視覺效果
                const style = document.createElement('style');
                style.textContent = `
                    .container { 
                        box-shadow: 0 15px 40px rgba(0,0,0,0.2) !important; 
                    }
                    .stat-card:hover { 
                        transform: translateY(-4px) !important; 
                        box-shadow: 0 8px 25px rgba(0,0,0,0.15) !important; 
                    }
                    tbody tr:hover { 
                        transform: scale(1.003) !important; 
                    }
                `;
                document.head.appendChild(style);
            }

            // 超寬螢幕特別處理
            if (aspectRatio >= 2.0) {
                if (statsGrid) {
                    statsGrid.style.gridTemplateColumns = 'repeat(4, 1fr)';
                    statsGrid.style.maxWidth = '80%';
                    statsGrid.style.margin = '0 auto';
                }
            }
        }

        // 移除數字動畫效果 - 直接顯示結果

        // 檢查是否需要調整字體大小以避免換行
        function optimizeTextFitting() {
            const tcNameCells = document.querySelectorAll('.tc-name');

            tcNameCells.forEach(cell => {
                // 如果內容太長，動態調整字體大小
                const originalFontSize = window.getComputedStyle(cell).fontSize;
                const textLength = cell.textContent.length;

                if (textLength > 30 && window.innerWidth <= 768) {
                    cell.style.fontSize = 'clamp(9px, 2vw, 12px)';
                    cell.style.lineHeight = '1.2';
                } else if (textLength > 50 && window.innerWidth <= 1024) {
                    cell.style.fontSize = 'clamp(10px, 1.8vw, 13px)';
                    cell.style.lineHeight = '1.3';
                }
            });
        }

        // 初始化
        document.addEventListener('DOMContentLoaded', function() {
            adaptSummaryToResolution();
            optimizeTextFitting();

            console.log('[No-Scroll Summary Adapter] Initialized for', window.innerWidth + 'x' + window.innerHeight);
        });

        // 視窗大小改變時更新
        let summaryResizeTimeout;
        window.addEventListener('resize', function() {
            clearTimeout(summaryResizeTimeout);
            summaryResizeTimeout = setTimeout(() => {
                adaptSummaryToResolution();
                optimizeTextFitting();
            }, 100);
        });

        // 方向改變時重新計算
        window.addEventListener('orientationchange', function() {
            setTimeout(() => {
                adaptSummaryToResolution();
                optimizeTextFitting();
            }, 300);
        });
        </script>
        
        </body>
        </html>